{
  "name": "hermeswork-backend-python",
  "version": "12.0.0",
  "description": "HermesWork v12.0 - Full Python/FastAPI backend | 41 agents, 68 MCP tools, 41 research papers",
  "main": "app.py",
  "scripts": {
    "start": "uvicorn app:app --host 0.0.0.0 --port $PORT",
    "dev": "uvicorn app:app --host 0.0.0.0 --port 3500 --reload",
    "test": "python3 test_suite.py",
    "check": "python3 -c 'import py_compile; py_compile.compile(\"app.py\", doraise=True); print(\"OK\")'"
  },
  "engines": {
    "python": ">=3.11"
  }
}