/* ======================================================================
   HermesWork v2.1 — Settings/API-key helpers (loaded by app.js logic)
   This file documents the helpers now built into app.js:
   - getApiKey(): reads HERMESWORK_API_KEY from localStorage
   - saveApiKeyFromField(): saves the Settings field value to localStorage
   - prefillSettings(): fills the Settings fields on load
   The canonical implementation lives in app.js. This file is reference only.
   ====================================================================== */
